name: Labeler

on:
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    name: Apply PR labels
    runs-on: ubuntu-latest

    steps:
      - name: Label pull request
        uses: actions/labeler@v6
