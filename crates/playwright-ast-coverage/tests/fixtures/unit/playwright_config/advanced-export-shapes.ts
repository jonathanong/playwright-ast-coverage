const { ignored } = {};
let empty;
void 0;

const config = {
  testDir: './advanced-export-tests',
  testMatch: ['**/*.advanced-export.ts'],
};

exports.config = config;
exports = config;

export default defineConfig((config));
