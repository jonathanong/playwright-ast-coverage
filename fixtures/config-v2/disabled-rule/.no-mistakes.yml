projects:
  backend:
    type: server
    root: backend
rules:
  - rule: active-rule
    projects:
      - backend
    message: This rule is active
  - rule: disabled-rule
    projects:
      - backend
    enabled: false
    message: This rule is disabled
