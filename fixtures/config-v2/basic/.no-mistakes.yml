projects:
  backend:
    type: server
    root: backend
    include:
      - backend/**
    routes:
      - backend/api/**
rules:
  - rule: http-route-static-paths
    projects:
      - backend
    message: Route paths must be static literals
