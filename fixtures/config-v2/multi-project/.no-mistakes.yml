filesystem:
  skipDirectories:
    - .next
    - node_modules
projects:
  backend:
    type: server
    root: backend
    include:
      - backend/**
    routes:
      - backend/api/**
    queues:
      enqueues:
        - backend/queues/**
      workers:
        - backend/workers/**
  web:
    type: nextjs
    root: web
    include:
      - web/**
tests:
  playwright:
    configs: playwright.config.ts
    selectors:
      htmlIds: false
      testIds:
        - data-testid
        - data-pw
      componentTestIds:
        dataPw: data-pw
    selectorRoots:
      - web/app
      - web/components
    selectorExclude:
      - '**/*.test.tsx'
      - '**/*.stories.tsx'
  vitest:
    configs: vitest.config.ts
rules:
  - rule: http-route-static-paths
    projects:
      - backend
    message: Route paths must be static literals
    options:
      backendPattern: backend/api/**
  - rule: no-dynamic-server-components
    projects:
      - web
    message: No dynamic server components allowed
