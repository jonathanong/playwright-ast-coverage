rules:
  test-structure:
    mockingMethods:
      - mock
      - spyOn
      - fn
      - clearAllMocks
      - restoreAllMocks
    mockImplementationMethods:
      - mockResolvedValue
      - mockRejectedValue
      - mockReturnValue
      - mockImplementation
